#include <bits/stdc++.h>

using namespace std;
typedef long long int ll;

int main() {
	ll n;
	scanf("%ld", &n);

	ll upperBound = 0, lowerBOund = 0;
	for (int i = 0; i < n; i++) {
		string instr;
		char num, ans;
		scanf("")
	}

	return 0;
}