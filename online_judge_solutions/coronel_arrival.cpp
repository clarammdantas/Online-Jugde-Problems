//general arrival - codeforces-div2A

#include <bits/stdc++.h>

using namespace std;

int main(){
	int n;
	cin >> n;
	
	int heights[n];
	int bigest = 0;
	int smaller = -1;
	for(int i = 0; i < n; i++){
		int x;
		hights[i] = x;
		if(x > bigest) bigest = x;
		if(x < smaller) smaller = x;
	}
	
	executions = 0;
	for(){
		
	}
	
	return 0;
}
